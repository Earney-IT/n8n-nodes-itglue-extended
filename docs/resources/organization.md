# Organization

> Generated by `scripts/gen-docs.ts`. Do not edit manually — re-run `npm run docs` to regenerate.

| Property | Value |
|---|---|
| **Resource name** | `organization` |
| **JSON:API type** | `organizations` |
| **Endpoint** | `/organizations` |



## Operations

| Operation | Description |
|---|---|
| `getAll` | List / search all records (paginated) |
| `get` | Retrieve a single record by ID |
| `create` | Create a new record |
| `update` | Update an existing record |
| `delete` | Delete a record by ID |
| `bulkDelete` | Delete multiple records in a single request |

## Fields

| n8n Param | JSON:API Attribute | Type | Required | On Operations | Description |
|---|---|---|---|---|---|
| `alert` | `alert` | `string` | No | `create`, `update` | An alert message for the organization |
| `description` | `description` | `string` | No | `create`, `update` | A description of the organization |
| `name` | `name` | `string` | Yes | `create`, `update` | The organization name |
| `organizationStatusId` | `organization-status-id` | `options` | No | `create`, `update` | The organization status |
| `organizationTypeId` | `organization-type-id` | `options` | No | `create`, `update` | The organization type |
| `quickNotes` | `quick-notes` | `string` | No | `create`, `update` | Quick notes for the organization |

## Filters

Available on the `getAll` operation via the **Filters** parameter.

| Filter Name | JSON:API Attribute | Type | Description |
|---|---|---|---|
| `name` | `name` | `string` | Filter by organization name |
| `organizationStatusId` | `organization-status-id` | `string` | Filter by organization status ID |
| `organizationTypeId` | `organization-type-id` | `string` | Filter by organization type ID |

## Using with AI Agents

This node is `usableAsTool: true`. When invoked from an AI/agent workflow the node behaves identically to a manual workflow, **except**:

- **Passwords are fail-closed.** Any field marked as a secret (e.g. `revealPlaintext`) is ignored when the node is running as a tool — plaintext secrets are never returned to an AI agent regardless of field values.
- Agents can create, update, rotate, and delete records normally.
- Safe agent prompt example: *"Find the organization named X in org Y"*
