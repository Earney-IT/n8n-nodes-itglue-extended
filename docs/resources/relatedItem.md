# Related Item

> Generated by `scripts/gen-docs.ts`. Do not edit manually — re-run `npm run docs` to regenerate.

| Property | Value |
|---|---|
| **Resource name** | `relatedItem` |
| **JSON:API type** | `related_items` |
| **Endpoint** | `/related_items` |
| **ID param** | `relatedItemId` |
| **Special handler** | `relatedItems` |

> **Special handler:** `relatedItems` — uses a custom request pipeline instead of the generic CRUD engine.


## Operations

| Operation | Description |
|---|---|
| `create` | Create a new record |
| `update` | Update an existing record |
| `bulkDelete` | Delete multiple records in a single request |

## Fields

| n8n Param | JSON:API Attribute | Type | Required | On Operations | Description |
|---|---|---|---|---|---|
| `destinationId` | `destination-id` | `string` | Yes | `create` | The ID of the destination resource to relate to |
| `destinationType` | `destination-type` | `options` | Yes | `create` | The type of the destination resource |
| `notes` | `notes` | `string` | No | `create`, `update` | Notes about this relationship |
| `relatedItemIds` | `related-item-ids` | `json` | No | `bulkDelete` | JSON array of related item IDs to delete in bulk |
| `resourceId` | `resource-id` | `string` | Yes | `create`, `update`, `bulkDelete` | The ID of the source resource |
| `resourceType` | `resource-type` | `options` | Yes | `create`, `update`, `bulkDelete` | The type of the source resource for this relationship |

## Using with AI Agents

This node is `usableAsTool: true`. When invoked from an AI/agent workflow the node behaves identically to a manual workflow, **except**:

- **Passwords are fail-closed.** Any field marked as a secret (e.g. `revealPlaintext`) is ignored when the node is running as a tool — plaintext secrets are never returned to an AI agent regardless of field values.
- Agents can create, update, rotate, and delete records normally.
- Safe agent prompt example: *"Find the related item named X in org Y"*
