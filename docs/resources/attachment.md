# Attachment

> Generated by `scripts/gen-docs.ts`. Do not edit manually — re-run `npm run docs` to regenerate.

| Property | Value |
|---|---|
| **Resource name** | `attachment` |
| **JSON:API type** | `attachments` |
| **Endpoint** | `/attachments` |
| **ID param** | `attachmentId` |
| **Special handler** | `attachments` |

> **Special handler:** `attachments` — uses a custom request pipeline instead of the generic CRUD engine.


## Operations

| Operation | Description |
|---|---|
| `getAll` | List / search all records (paginated) |
| `get` | Retrieve a single record by ID |
| `create` | Create a new record |
| `update` | Update an existing record |
| `delete` | Delete a record by ID |
| `bulkDelete` | Delete multiple records in a single request |

## Fields

| n8n Param | JSON:API Attribute | Type | Required | On Operations | Description |
|---|---|---|---|---|---|
| `attachmentIds` | `attachment-ids` | `json` | No | `bulkDelete` | JSON array of attachment IDs to delete in bulk |
| `binaryPropertyName` | `binary-property-name` | `string` | No | `create` | Name of the n8n binary property to upload (alternative to File Base64) |
| `fileBase64` | `file-base-64` | `string` | No | `create` | Base64-encoded file content (or set Binary Property Name instead) |
| `fileName` | `file-name` | `string` | No | `create` | The file name for the attachment |
| `name` | `name` | `string` | No | `update` | The display name of the attachment |
| `resourceId` | `resource-id` | `string` | Yes | `getAll`, `get`, `create`, `update`, `delete`, `bulkDelete` | The ID of the parent resource |
| `resourceType` | `resource-type` | `options` | Yes | `getAll`, `get`, `create`, `update`, `delete`, `bulkDelete` | The type of the parent resource this attachment belongs to |

## Using with AI Agents

This node is `usableAsTool: true`. When invoked from an AI/agent workflow the node behaves identically to a manual workflow, **except**:

- **Passwords are fail-closed.** Any field marked as a secret (e.g. `revealPlaintext`) is ignored when the node is running as a tool — plaintext secrets are never returned to an AI agent regardless of field values.
- Agents can create, update, rotate, and delete records normally.
- Safe agent prompt example: *"Find the attachment named X in org Y"*
