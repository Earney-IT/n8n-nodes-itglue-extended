# Document

> Generated by `scripts/gen-docs.ts`. Do not edit manually — re-run `npm run docs` to regenerate.

| Property | Value |
|---|---|
| **Resource name** | `document` |
| **JSON:API type** | `documents` |
| **Endpoint** | `/documents` |
| **ID param** | `documentId` |
| **Special handler** | `documents` |

> **Special handler:** `documents` — uses a custom request pipeline instead of the generic CRUD engine.


## Operations

| Operation | Description |
|---|---|
| `getAll` | List / search all records (paginated) |
| `get` | Retrieve a single record by ID |
| `create` | Create a new record |
| `update` | Update an existing record |
| `delete` | Delete a record by ID |
| `publish` | Publish a draft record |

## Fields

| n8n Param | JSON:API Attribute | Type | Required | On Operations | Description |
|---|---|---|---|---|---|
| `content` | `content` | `string` | No | `create`, `update` | The document content (HTML or markdown) |
| `documentResource` | `document-resource` | `options` | Yes | `getAll`, `get`, `create`, `update`, `delete`, `publish` | The type of document resource to operate on |
| `imageId` | `image-id` | `string` | No | `get`, `update`, `delete` | The image ID (used when Document Resource is Image) |
| `name` | `name` | `string` | No | `create`, `update` | The document name |
| `parentDocumentId` | `document-id` | `string` | No | `create` | Parent document ID (required for Section/Image create) |
| `sectionId` | `section-id` | `string` | No | `get`, `update`, `delete` | The section ID (used when Document Resource is Section) |

## Using with AI Agents

This node is `usableAsTool: true`. When invoked from an AI/agent workflow the node behaves identically to a manual workflow, **except**:

- **Passwords are fail-closed.** Any field marked as a secret (e.g. `revealPlaintext`) is ignored when the node is running as a tool — plaintext secrets are never returned to an AI agent regardless of field values.
- Agents can create, update, rotate, and delete records normally.
- Safe agent prompt example: *"Find the document named X in org Y"*
