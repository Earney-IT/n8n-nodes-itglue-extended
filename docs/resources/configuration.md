# Configuration

> Generated by `scripts/gen-docs.ts`. Do not edit manually — re-run `npm run docs` to regenerate.

| Property | Value |
|---|---|
| **Resource name** | `configuration` |
| **JSON:API type** | `configurations` |
| **Endpoint** | `/configurations` |
| **Org-scoped** | Yes |


> **Organisation-scoped:** The node offers a "Scope to Organisation" toggle for this resource. When enabled, requests are prefixed with `/organizations/{id}/`.

## Operations

| Operation | Description |
|---|---|
| `getAll` | List / search all records (paginated) |
| `get` | Retrieve a single record by ID |
| `create` | Create a new record |
| `update` | Update an existing record |
| `delete` | Delete a record by ID |
| `bulkUpdate` | Update multiple records in a single request |

## Fields

| n8n Param | JSON:API Attribute | Type | Required | On Operations | Description |
|---|---|---|---|---|---|
| `archived` | `archived` | `boolean` | No | `create`, `update` | Whether the configuration is archived |
| `assetTag` | `asset-tag` | `string` | No | `create`, `update` | The asset tag of the configuration |
| `configurationStatusId` | `configuration-status-id` | `options` | No | `create`, `update` | The configuration status |
| `configurationTypeId` | `configuration-type-id` | `options` | No | `create`, `update` | The configuration type |
| `hostname` | `hostname` | `string` | No | `create`, `update` | The hostname of the configuration |
| `macAddress` | `mac-address` | `string` | No | `create`, `update` | The MAC address of the configuration |
| `name` | `name` | `string` | Yes | `create`, `update` | The configuration name |
| `notes` | `notes` | `string` | No | `create`, `update` | Notes about the configuration |
| `primaryIp` | `primary-ip` | `string` | No | `create`, `update` | The primary IP address of the configuration |
| `serialNumber` | `serial-number` | `string` | No | `create`, `update` | The serial number of the configuration |

## Filters

Available on the `getAll` operation via the **Filters** parameter.

| Filter Name | JSON:API Attribute | Type | Description |
|---|---|---|---|
| `archived` | `archived` | `boolean` | Filter by archived status |
| `configurationTypeId` | `configuration-type-id` | `string` | Filter by configuration type ID |
| `name` | `name` | `string` | Filter by configuration name |
| `organizationId` | `organization-id` | `string` | Filter by organization ID |
| `serialNumber` | `serial-number` | `string` | Filter by serial number |

## Includes (side-loads)

Pass one or more of the following values in the **Include** parameter to side-load related data in the same response:

- `configuration_interfaces`
- `attachments`
- `passwords`
- `related_items`
- `recent_versions`

## Using with AI Agents

This node is `usableAsTool: true`. When invoked from an AI/agent workflow the node behaves identically to a manual workflow, **except**:

- **Passwords are fail-closed.** Any field marked as a secret (e.g. `revealPlaintext`) is ignored when the node is running as a tool — plaintext secrets are never returned to an AI agent regardless of field values.
- Agents can create, update, rotate, and delete records normally.
- Safe agent prompt example: *"Find the configuration named X in org Y"*
