# Export

> Generated by `scripts/gen-docs.ts`. Do not edit manually — re-run `npm run docs` to regenerate.

| Property | Value |
|---|---|
| **Resource name** | `export` |
| **JSON:API type** | `exports` |
| **Endpoint** | `/exports` |
| **ID param** | `exportId` |
| **Special handler** | `exports` |

> **Special handler:** `exports` — uses a custom request pipeline instead of the generic CRUD engine.


## Operations

| Operation | Description |
|---|---|
| `getAll` | List / search all records (paginated) |
| `get` | Retrieve a single record by ID |
| `create` | Create a new record |
| `delete` | Delete a record by ID |
| `createAndWait` | Create and wait for completion (async) |

## Fields

| n8n Param | JSON:API Attribute | Type | Required | On Operations | Description |
|---|---|---|---|---|---|
| `format` | `format` | `options` | No | `create`, `createAndWait` | The export file format |
| `includeRelated` | `include-related` | `boolean` | No | `create`, `createAndWait` | Whether to include related resources in the export |
| `resourceId` | `resource-id` | `string` | No | `create`, `createAndWait` | The ID of the specific resource to export (optional) |
| `resourceType` | `resource-type` | `string` | No | `create`, `createAndWait` | The resource type to export (e.g. configurations, passwords) |

## Using with AI Agents

This node is `usableAsTool: true`. When invoked from an AI/agent workflow the node behaves identically to a manual workflow, **except**:

- **Passwords are fail-closed.** Any field marked as a secret (e.g. `revealPlaintext`) is ignored when the node is running as a tool — plaintext secrets are never returned to an AI agent regardless of field values.
- Agents can create, update, rotate, and delete records normally.
- Safe agent prompt example: *"Find the export named X in org Y"*
